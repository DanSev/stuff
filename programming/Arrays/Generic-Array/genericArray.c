#include <stdio.h>
#include <stdlib.h>
#include "genericArray.h"


int findDuplicatesInt(int* array, int arraySize, int** dubIdx)
{
		/* Dummy Code to show usage of test macros
		 */

		*dubIdx = malloc(sizeof(**dubIdx * 1));
		*dubIdx[0] = 3;

		return 1;
}

